{
  "name": "sentiment-demo",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "idyll index.idyll --defaultComponents=components/default --css styles.css --watch",
    "build": "idyll index.idyll --defaultComponents=components/default --css styles.css",
    "deploy": "npm run build && gh-pages -d ./build"
  },
  "dependencies": {
    "d3": "^4.0.0",
    "idyll": "^2.0.0",
    "idyll-d3-component": "^2.0.0",
    "idyll-vega-lite": "^1.0.2",
    "vega": "^4.0.0-rc.1",
    "vega-lite": "^2.4.3"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0"
  }
}
